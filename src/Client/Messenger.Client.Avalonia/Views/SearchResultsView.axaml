<!-- Разметка экрана BitCanary «SearchResultsView»: поля, списки и панели (Avalonia). -->
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Messenger.Client.Avalonia.ViewModels"
             x:Class="Messenger.Client.Avalonia.Views.SearchResultsView"
             x:DataType="vm:SearchViewModel">
  <Grid RowDefinitions="Auto,Auto,*">

    
    <Border Grid.Row="0"
            BorderBrush="#1E1E1E"
            BorderThickness="0,0,0,1"
            Padding="12,8">
      <TextBlock Text="SEARCH"
                 FontSize="14" FontWeight="Bold"
                 Foreground="{DynamicResource SystemControlHighlightAccentBrush}" />
    </Border>

    
    <Border Grid.Row="1"
            BorderBrush="#1E1E1E"
            BorderThickness="0,0,0,1"
            Padding="10,6">
      <TextBox Watermark="search messages or @users..."
               Text="{Binding SearchQuery, Mode=TwoWay}"
               Background="Transparent"
               BorderThickness="0"
               FontSize="14"
               Name="SearchQueryBox" />
    </Border>

    
    <Panel Grid.Row="2">

      <ProgressBar IsIndeterminate="True"
                   IsVisible="{Binding IsLoading}"
                   VerticalAlignment="Top" />

      
      <TextBlock Text="start typing to search your messages"
                 FontSize="14" Foreground="#404040"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 IsVisible="{Binding !HasSearched}" />

      
      <StackPanel HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Spacing="4"
                  IsVisible="{Binding ShowNoResults}">
        <TextBlock Text="{Binding EmptyStateHeading}"
                   FontSize="14" Foreground="#888888"
                   HorizontalAlignment="Center" />
        <TextBlock Text="{Binding EmptyStateBody}"
                   FontSize="14" Foreground="#555555"
                   HorizontalAlignment="Center" />
      </StackPanel>

      
      <ListBox ItemsSource="{Binding SearchResults}"
               IsVisible="{Binding ShowMessageResults}"
               SelectionMode="Single"
               Background="Transparent"
               BorderThickness="0"
               Name="ResultsList">
        <ListBox.ItemTemplate>
          <DataTemplate x:DataType="vm:SearchResultItemViewModel">
            <Border BorderBrush="#141414" BorderThickness="0,0,0,1" Padding="12,8">
              <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <StackPanel Spacing="3">
                  <TextBlock Text="{Binding ChatName}"
                             FontSize="14" FontWeight="Bold"
                             Foreground="{DynamicResource SystemControlHighlightAccentBrush}" />
                  <TextBlock Text="{Binding Snippet}"
                             FontSize="14" Foreground="#888888"
                             TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <TextBlock Grid.Column="1"
                           Text="{Binding Timestamp}"
                           FontSize="14" Foreground="#444444"
                           VerticalAlignment="Top" />
              </Grid>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      
      <ListBox ItemsSource="{Binding UserResults}"
               IsVisible="{Binding ShowUserResults}"
               SelectionMode="Single"
               Background="Transparent"
               BorderThickness="0"
               Name="UserResultsList">
        <ListBox.ItemTemplate>
          <DataTemplate x:DataType="vm:UserResultItemViewModel">
            <Border BorderBrush="#141414" BorderThickness="0,0,0,1" Padding="12,8">
              <StackPanel Spacing="3">
                <TextBlock Text="{Binding DisplayName}"
                           FontSize="14" FontWeight="Bold"
                           Foreground="{DynamicResource SystemControlHighlightAccentBrush}" />
                <TextBlock Text="{Binding Username}"
                           FontSize="14" Foreground="#888888"
                           TextTrimming="CharacterEllipsis" />
              </StackPanel>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

    </Panel>
  </Grid>
</UserControl>
